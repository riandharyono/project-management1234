{
  "project_name": "Project Management",
  "created_by": "R",
  "theme_preference": "light",
  "design_system_name": "Ledger",
  "typography": {
    "headings": "Manrope",
    "body": "IBM Plex Sans, sans-serif",
    "mono": "IBM Plex Mono, monospace",
    "hierarchy": {
      "h1": "28px / 800 tracking-tight",
      "h2": "20px / 700 tracking-tight",
      "h3": "14px / 700",
      "body": "13px / 1.5",
      "small": "11px / 600"
    },
    "guidelines": "Manrope for headings, IBM Plex Sans for body copy. IBM Plex Mono is the signature accent face for metadata: counts, dates, priority/status pills, stat numbers, kbd hints, breadcrumbs — it is what separates this from a generic dashboard."
  },
  "colors": {
    "archetype": "Ledger — warm editorial workbench, high contrast",
    "variables": {
      "--paper": "#F6F4EF",
      "--surface": "#FFFFFF",
      "--sidebar": "#0C0B09",
      "--sidebar-panel": "#17140F",
      "--ink": "#17140F",
      "--muted": "#6B6355",
      "--line": "#E3DFD3",
      "--accent": "#E8531F",
      "--accent-hover": "#C7431A",
      "--accent-tint": "#FDE4D8",
      "--success": "#1F7A52",
      "--success-tint": "#E1F3E9",
      "--danger": "#C63C2A",
      "--danger-tint": "#FBE4DF",
      "--warning": "#B9781A",
      "--warning-tint": "#FBEEDA",
      "--violet": "#6355C7",
      "--violet-tint": "#EDEBFB",
      "--teal": "#0E8A82",
      "--teal-tint": "#E1F4F2",
      "--rose": "#C23A63",
      "--rose-tint": "#FBE3EA"
    },
    "specific_adjustments": {
      "sidebar_bg": "#0C0B09",
      "sidebar_text_active": "#FFFFFF",
      "sidebar_text_inactive": "#A99C87",
      "sidebar_active_indicator": "2px solid var(--accent) inset-left",
      "primary_button_bg": "#E8531F",
      "primary_button_hover": "#C7431A",
      "borders": "#E3DFD3",
      "muted_text": "#6B6355"
    },
    "guidelines": "One confident accent (Ember orange) replaces the previous generic blue. Warm paper background (#F6F4EF) with white surfaces on top creates layering instead of flat white-on-white. Sidebar is a flat near-black (no gradient). Semantic colors (success/danger/warning) and the violet/teal/rose overview-card tones all sit on the same warm-neutral base."
  },
  "spacing_and_layout": {
    "strategy": "High Density Grid — Compact and Scannable",
    "radius_scale": {
      "sm": "6px (buttons, inputs, pills, tags)",
      "md": "8px (cards, columns, panels)",
      "lg": "12px (modals only)"
    },
    "guidelines": "Cards are flat (1px border, no ambient shadow) at rest; elevation (lift + shadow) appears only on hover/focus. Keep padding compact and consistent with the existing density (task cards ~11px, page padding ~28px 4.5%)."
  },
  "motion": {
    "guidelines": "Every interactive element gets a targeted transition (never `transition: all`): buttons lift on hover and scale down slightly on press; tabs and nav items animate background/color; kanban cards, overview cards, and list rows fade+rise in on mount (`riseIn` keyframe); modals and dropdowns fade+scale in (`modalIn`); the unread notification dot pulses (`pulseDot`). Keyframes live in index.css and are reused across App.css/team.css/extra.css."
  },
  "components": {
    "kanban_cards": {
      "guidelines": "Tight information density, mono font for meta (due date, counts). Solid 1px var(--line) borders; hover lifts the card and darkens the border, never changes at rest."
    },
    "sidebar": {
      "guidelines": "Flat near-black background, no gradient. Active state uses a dark panel fill plus a crisp 2px accent (Ember) inset-left indicator and white text."
    },
    "topbar": {
      "guidelines": "58px height to preserve vertical space for the board/lists. Active tab uses the accent tint background + accent-hover text."
    },
    "task_detail_modal": {
      "guidelines": "Compact padding, low-profile inputs (8-10px). Mono font for breadcrumb, labels, and section eyebrows."
    }
  },
  "instructions_to_main_agent": [
    "1. Do NOT touch or remove any existing `data-testid` attributes in the JS files.",
    "2. Do NOT change the LABEL_COLORS swatch palette values (CreateTeamModal.jsx / TaskDetailModal.jsx) — they are embedded in generated data-testid strings.",
    "3. All visual styling lives in App.css, extra.css, team.css and the font import in index.css — prefer editing those over adding inline styles or Tailwind classes to JSX.",
    "4. Keep class names stable; only change declarations inside existing rules or add new rules for a genuinely new class.",
    "5. Reuse the CSS custom properties defined in App.css :root (--paper, --surface, --ink, --muted, --line, --accent, --accent-hover, --accent-tint, --success, --danger, --warning, --violet, --teal, --rose, --r-sm, --r-md, --r-lg, --font-heading, --font-body, --font-mono) instead of hardcoding new hex values."
  ],
  "UNIVERSAL GUIDELINES FOR MAIN AGENT": [
    "You tend to converge toward generic, 'on distribution' outputs. Avoid this: make creative, distinctive frontends.",
    "Dark colors look good when used independently without gradients. DO NOT use dark colors as gradients.",
    "Create depth through layered design elements with z-index hierarchy, but keep cards sharp for dense data tools.",
    "You MUST NOT apply universal transition. Eg: transition: all. Always add transitions for specific interactive elements.",
    "You MUST NOT center align the app container, ie do not add .App { text-align: center; } in the css file.",
    "Always use modern, sharp button styles (e.g., 6px radius) with interaction animations for dashboard context.",
    "NEVER use AI assistant Emoji characters like 🤖🧠💭💡🔮🎯 for icons.",
    "Every interaction needs micro-animations - hover states, transitions. Static = dead."
  ]
}
